{
  "name": "pirate-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.tests.json",
    "test": "vite build && bun test --timeout 30000"
  },
  "dependencies": {
    "ghostty-web": "0.4.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "playwright": "1.57.0",
    "typescript": "7.0.2",
    "vite": "8.2.0"
  }
}
